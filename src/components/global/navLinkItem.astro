---
const linkStyles = 'font-semibold uppercase text-base';
const { text, url, active, variation, className = '' } = Astro.props;
---

<li>
	<a
		href={url}
		class={`nav-item ${linkStyles} ${
			active ? 'border-b-2 border-green-600' : ''
		} ${className}}`}
	>
		{text}
	</a>
</li>
