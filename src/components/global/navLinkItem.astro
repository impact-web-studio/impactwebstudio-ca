---
const { text, url, active, className = '' } = Astro.props;
---

<li>
	<a
		href={url}
		class={`${'text-gray-900'} ${
			active ? 'border-b-2 border-green-600' : ''
		} ${className}}`}
	>
		{text}
	</a>
</li>
