---
const { text, url, type, active, className = '' } = Astro.props;
---

<li>
	<a
		href={url}
		class={`${type === 'cta' ? 'text-white bg-green-600' : 'text-gray-900'} ${
			active ? 'border-b-2 border-green-600' : ''
		} ${className}}`}
	>
		{text}
	</a>
</li>
