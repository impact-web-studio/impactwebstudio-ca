---
const currentYear = new Date().getFullYear();

const { footerMetaData, copyright } = {
	footerMetaData: 'w-full',
	copyright:
		'flex justify-center items-center text-light-permanent/50 text-xs md:text-sm',
};
---

<div class=`${footerMetaData}`>
	<p class=`${copyright}`>Impact Web Studio © {currentYear}</p>
</div>
